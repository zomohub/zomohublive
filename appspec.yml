version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html  # Change this to your destination directory

hooks:
  AfterInstall:
    - location: set_permissions.sh  # Path to the script that sets permissions
      runas: root

